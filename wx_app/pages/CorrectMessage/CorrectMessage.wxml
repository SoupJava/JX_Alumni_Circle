<!--pages/CorrectMessage/CorrectMessage.wxml-->
<van-notify id="van-notify" />
<view class="regist">
  <view class="welcome">
    <view class='eye'>
      <view class="left">
        <view class="leftl"></view>
      </view>
      <view class="right">
        <view class="rightl"></view>
      </view>
    </view>
  </view>
  <view class="bg">
    <view class='box'>
      <view style="display: flex;align-items: center;justify-content: center;">
        <van-uploader file-list="{{ userImage }}" max-count="1" bind:after-read="afterRead" bind:delete="deleteImg" deletable="{{ true }}" />
      </view>
      <van-cell-group>
        <van-field model:value="{{ username }}" clearable left-icon="smile" label="用户名" placeholder="请输入用户名" bind:click-icon="onClickIcon" />
      </van-cell-group>
      <van-cell-group>
        <van-field model:value="{{ jxid }}" clearable disabled left-icon="manager" label="账号" placeholder="请输入账号" bind:click-icon="onClickIcon" />
      </van-cell-group>
      <van-cell-group>
        <van-field model:value="{{ school }}" clearable disabled left-icon="location" label="学校" placeholder="请输入学校名称" bind:click-icon="onClickIcon" />
      </van-cell-group>
      <van-cell-group>
        <van-field model:value="{{ telephone }}" clearable left-icon="phone-circle" label="手机号" type="number" placeholder="请输入手机号" bind:click-icon="onClickIcon" />
      </van-cell-group>
      <van-cell-group>
        <van-field model:value="{{ password }}" type="password" left-icon="youzan-shield" label="密码" placeholder="请输入密码" border="{{ false }}" />
      </van-cell-group>
      <van-cell-group>
        <van-field model:value="{{ againpassword }}" type="password" label="确认密码" left-icon="umbrella-circle" placeholder="请确认密码" border="{{ false }}" />
      </van-cell-group>
      <view class="sex">
        <van-icon name="friends" />
        <view class="label">性别</view>
        <view class="sel">
          <view class="sel2">
            <van-radio-group value="{{ sex }}" bind:change="ChangeSex" direction="horizontal">
              <van-radio name="0" icon-size="30rpx">
                <view class="text">
                  男
                </view>
              </van-radio>
              <van-radio name="1" icon-size="30rpx" style="margin-left:20rpx;">
                <view class="text">
                  女
                </view>
              </van-radio>
            </van-radio-group>
          </view>
        </view>
      </view>
      <van-button type="primary" size="large" bind:tap="keep">保 存</van-button>
    </view>
  </view>
</view>
<van-toast id="van-toast" />